static const int numThreads = 8;

RWStructuredBuffer<float4> points;
float3 numVoxelsPerAxis;
float3 worldBounds;
float3 chunkSize;
float3 chunkCenter;
float voxelSize;

int indexFromCoord(uint x, uint y, uint z) {
    return z * numPointsPerAxis * numPointsPerAxis + y * numPointsPerAxis + x;
}